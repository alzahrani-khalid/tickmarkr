export const C1_CRITERIA = [
  "The changed runLiveCockpit production mount and capture path render C3 Home, C4 Run and C5 Evidence through C2 snapshots at 120x40 and 80x24 and validate every integer 40–220 by 14–50 size against FINAL §3.1. Rendered MERGED, CURRENT TIP PENDING, status and q Quit fit their cell/row budgets, with 64-character task IDs, CJK/combining text and a 200-line verdict reachable by paging. At 39x14 and 40x13 a size refusal still quits. Existing five-row StatTiles overflowing the 17-row narrow body or a leaf seen only in its unit test fail.",
  "The mounted input and pointer paths share the FINAL §3.3 registry and committed geometry: keys 1, 4 and 5 select installed views, Tab and Shift-Tab skip folded regions at widths 80/90/120, and each advertised action produces its transition. A q1? batch remains literal editor text, y alone confirms after review, n/Esc cancels, dirty q opens staged-edit review, and Esc closes the deepest overlay. A text digit jumping views, hidden focus target or inert displayed key fails.",
  "The production terminal mount repays its raw, pointer, title and alternate-screen loans and stops its own timers/listeners on q, Ctrl-C, SIGTERM and induced read/render failure, proven by captured entry/restore sequences and subsequent usable input. Two independent mounts keep the surviving observer functional. An error branch or signal that leaves a terminal mode loaned, or quits merely on Esc, fails.",
  "The mounted frames use FINAL §3.2 palette and explicit words for passed, running, human park, terminal failure, unknown and disabled across truecolor, reduced color and NO_COLOR. A measured magnitude changes the guarded green ramp independently of tile identity. C1 updates working captures and manifest membership through an explicit contract-moved retirement while retaining protected anchor/source bytes. A running or human checkmark, decorative trend with no history, changed one-shot TOKENS, or blanket golden regeneration fails.",
  "The changed production live mount passes the exact memory protocol in the spec preamble in static, growing and resize cases: after 1000 warm-up ticks, all 10000 measured ticks retain at most 64 MiB post-GC heap and final-2000-tick growth at most 16 MiB, with actual render writes and a bounded sink. The normal CLI environment must pass as well as the production-mode control. The measured default-runtime performance-timeline growth, a test-only environment switch, or a harness retaining every frame fails."
] as const;
