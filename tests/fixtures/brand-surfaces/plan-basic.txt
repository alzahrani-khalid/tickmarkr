tickmarkr plan — dry run (16 channels available)
mode: risk-based (default) · explore on
driver: auto → subprocess (HERDR_ENV unset)
harness: /opt/tickmarkr/lib/node_modules/tickmarkr/dist/cli/index.js (unresolved location)

  T1     chore      c2  → claude-code:haiku [sub/cheap] — floor cheap (config floors), marginal-cost auto (cheapest sufficient tier)
    judge: claude-code:fable
    review: codex:gpt-5.6-sol — reviewPolicy full
    floor cheap ← mode risk-based

est. cost (API LLM seats, rough): ~$0.00 + consult calls

routing lints:
  ! claude-code: no model-list surface — seeds stamped 2026-07-09; verify manually
  ! codex: no detection data — run tickmarkr doctor
  ! cursor-agent: no detection data — run tickmarkr doctor
  ! opencode: no detection data — run tickmarkr doctor
  ! pi: no detection data — run tickmarkr doctor
  ! qwen: no model-list surface — seeds stamped 2026-07-09; verify manually
  ! agy: no model-list surface and no configured models — classify per benchmark policy to open channels