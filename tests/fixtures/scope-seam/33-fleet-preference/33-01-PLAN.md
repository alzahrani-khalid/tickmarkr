---
depends_on: []
files_modified:
  - tests/route/matrix.test.ts
autonomous: true
must_haves:
  truths:
    - The pre-FLEET-PREF discovery output and route matrix are pinned in
      committed tests BEFORE any implementation diff lands
    - The parity oracles go red if discoverChannels or route() output changes by
      one byte for an unset preference
---

```
<objective>
Pin the byte-identical parity baselines (oracles V-1, V-2) for Phase 33 BEFORE the allow/deny implementation exists.
</objective>
<done>V-1 and V-2 baselines committed and green against pre-implementation HEAD; red-capability proven via the injected-filter drill and reverted.</done>
<task>
```

