---
depends_on:
  - 33-02
files_modified:
  - src/route/preference.ts
  - src/cli/commands/plan.ts
  - src/cli/commands/doctor.ts
  - tests/cli/plan.test.ts
  - tests/cli/doctor.test.ts
autonomous: true
must_haves:
  truths:
    - drover plan output with an active preference names the excluded channels
      and the excluding reason
    - drover doctor output with an active preference shows the same exclusion
      line
    - With no preference set, plan and doctor output are byte-identical to
      today's (no new line, no changed line)
---

```
<objective>
Make the active fleet preference visible (oracle V-10): `drover plan` and `drover doctor` print one line naming each preference-excluded channel and why — and print nothing new when no preference is set.
</objective>
<done>Plan and doctor reflect the active preference (channel + reason), stay byte-identical when unset, and V-10 pins both directions.</done>
<task>
```

