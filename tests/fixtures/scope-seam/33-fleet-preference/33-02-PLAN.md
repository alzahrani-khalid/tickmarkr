---
depends_on:
  - 33-01
files_modified:
  - src/config/config.ts
  - src/route/preference.ts
  - src/adapters/registry.ts
  - src/route/router.ts
  - tests/config/config.test.ts
  - tests/route/preference.test.ts
autonomous: true
must_haves:
  truths:
    - An operator config with routing.allow/routing.deny removes exactly the
      disallowed channels from discovery; unset blocks change discovery and
      routing by zero bytes
    - A task pin, map pin, or prefer entry targeting a disallowed channel THROWS
      RoutingError at plan time naming both the channel and the excluding
      preference — never a silent degrade
    - A malformed allow/deny block fails config load with ConfigError; an empty
      allow block means nothing-allowed (fail-closed)
---

```
<objective>
Implement FLEET-06 + FLEET-07: additive optional `routing.allow`/`routing.deny` config, a single pure predicate `disallowedBy`, the discovery filter (with byte-identical early return), and the fail-loud hint preflight in `route()` — plus the red-capable oracles V-3..V-9.
</objective>
<done>Schema additive and fail-closed; all four V-9-class behaviors pinned green; template documents the grammar and semantics.</done>
<done>Predicate + both call sites shipped; V-3..V-8 and the nextChannel containment oracle green; parity baselines from 33-01 untouched and green; coverage gate passes.</done>
<task>
<task>
```

