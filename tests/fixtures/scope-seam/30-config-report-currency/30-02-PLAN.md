---
depends_on:
  - 30-01
files_modified:
  - src/config/config.ts
  - src/route/profile.ts
  - src/route/router.ts
  - src/run/journal.ts
  - tests/route/profile.test.ts
  - tests/config/config.test.ts
autonomous: true
must_haves:
  truths:
    - routing.learnedTuning.halfLifeRuns / .availWeight (optional sibling of
      routing.learned) reach buildProfile and learnedScore as pure params
    - With NO learnedTuning config, learned scores are Object.is-identical to
      today's module constants (plumbing only, not tuning)
    - An override demonstrably changes a warm cell's score (plumbing is not
      inert)
    - zod fails closed on a non-numeric learnedTuning override
    - "The ROUTE-12 penalty-only fork is resolved on the record: comment pin in
      profile.ts at the availability term, grep-pin oracle guards it"
---

```
<objective>
ROUTE-15: make the learning constants config-reachable via an ADDITIVE OPTIONAL sibling `routing.learnedTuning: { halfLifeRuns?, availWeight? }`, threaded as PURE PARAMS into `buildProfile`/`learnedScore` — defaults byte-identical to today's module constants.
</objective>
<done>All three new oracles green AND red-capable (drill comments name what reddens each); config-absent path Object.is-identical (oracle a + parity.test.ts green); non-numeric override throws ConfigError; full `npm test` green; zero new runtime deps. The preview-thread residual is CLOSED by 30-03, not deferred.</done>
<done>Resolution comment present at the avail term citing the real DECISIONS.md entry; grep pin green and red-capable (deleting the comment reddens it); all pre-existing ROUTE-12 tests green with zero behavior diff.</done>
<context>
@src/config/config.ts
@src/route/profile.ts
@src/route/router.ts
@src/run/journal.ts
@tests/route/profile.test.ts
@tests/route/parity.test.ts
</context>
<task>
<task>
```

