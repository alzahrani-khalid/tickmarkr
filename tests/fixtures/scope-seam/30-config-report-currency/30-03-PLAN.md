---
depends_on:
  - 30-02
files_modified:
  - src/cli/commands/profile.ts
  - tests/cli/profile.test.ts
autonomous: true
must_haves:
  truths:
    - "`drover profile show` renders via cellsOf() — profile.ts's cellKey format
      has exactly one parser (route/profile.ts:151)"
    - Rendered output is byte-identical pre/post refactor when no learnedTuning
      is set (golden test captured PRE-refactor)
    - cli/commands/profile.ts contains no hand-split of a cell key (no
      lastIndexOf('|') / split('|'))
    - The preview honors routing.learnedTuning.availWeight — preview score
      matches actual routing, never the module default when an override is set
---

```
<objective>
Replace the hand-rolled `key.lastIndexOf("|")` cell-key parsing in `src/cli/commands/profile.ts:50-59` with the one authority, `cellsOf()` (route/profile.ts:151) — mirroring how `report.ts:64` already consumes it (VIS-06).
</objective>
<done>Golden test green against UNREFACTORED show(); committed before any src change (git history proves capture order).</done>
<done>show() consumes cellsOf; golden output byte-identical with no override (Task 1 test green, unmodified); preview score reflects a learnedTuning.availWeight override (pin green, red-capable); no `.lastIndexOf("|")`/`.split("|")` in profile.ts (grep pin green, witnessed RED pre-refactor); full `npm test` green.</done>
<context>
@src/cli/commands/profile.ts
@src/cli/commands/report.ts
@tests/cli/profile.test.ts
</context>
<task>
<task>
```

