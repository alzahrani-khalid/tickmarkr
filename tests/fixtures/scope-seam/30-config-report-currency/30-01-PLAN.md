---
depends_on: []
files_modified:
  - src/config/config.ts
  - .drover/config.yaml
  - tests/config/config.test.ts
autonomous: true
must_haves:
  truths:
    - DEFAULT_CONFIG seeds grok-4.5-xhigh:mid and grok-4.5-fast-xhigh:cheap
      under cursor-agent with benchmark provenance
    - .drover/config.yaml no longer duplicates the grok seeds (no drift between
      overlay and source)
    - "The value-pin test was demonstrably RED before the seed landed
      (falsification discipline, DECISIONS #92)"
---

```
<objective>
Promote the grok-4.5 seeds from the repo-local `.drover/config.yaml` overlay into `DEFAULT_CONFIG.tiers["cursor-agent"].models`, carrying the benchmark provenance, then remove the now-redundant overlay lines so the two never drift (MODEL-10).
</objective>
<done>Both value pins green; git history shows the test commit red-first (RED commit precedes the seed commit); `npm run build` clean.</done>
<done>Overlay carries no grok lines; loadConfig on this repo still resolves both grok tiers (from DEFAULT_CONFIG); setup/taskTimeoutMinutes overlay values intact; full `npm test` green.</done>
<context>
@.planning/ROADMAP.md
@src/config/config.ts
@.drover/config.yaml
@tests/config/config.test.ts
</context>
<task>
<task>
```

