---
depends_on: []
files_modified:
  - src/adapters/opencode.ts
  - tests/adapters/opencode-usage.test.ts
autonomous: true
must_haves:
  truths:
    - opencode token usage is measured from message rows in
      ~/.local/share/opencode/opencode.db — never the dead storage/message JSON
      legacy, never the session table's cumulative tokens_* columns
    - The epoch-ms time.created cursor is compared as a NUMBER (no Date.parse) —
      a record at cursor 0 is kept, per-attempt slices are correct
    - cost:0 is never read as spend and no $ figure is fabricated — the sub
      channel buckets subscription via the existing v1.7 report path
    - sqlite3 binary absent / db missing / malformed rows ⇒ undefined
      (unmetered), never a throw, never {input:0}
    - reasoning is never populated (tokens.reasoning ⊂ output would double-count
      in report total())
---

```
<objective>
SPEND-09: implement `collectUsage(cwd, sinceMs)` on the opencode adapter, reading per-message token counts from opencode's own SQLite store (`opencode.db`) via `spawnSync("sqlite3", ["-json", …])` — zero new npm dependencies, tokens only, epoch-ms cursor, fail-open.
</objective>
<done>Test file exists, references opencode.collectUsage!, suite is RED (collectUsage not yet implemented).</done>
<done>opencode-usage suite fully green (all drills), full zero-token suite green (daemon.ts and report.ts untouched), tsc clean.</done>
<context>
@.planning/phases/29-fleet-metering/29-RESEARCH.md
@src/adapters/claude-code.ts
@src/adapters/types.ts
@tests/adapters/claude-usage.test.ts
</context>
<task>
<task>
```
Create `.planning/phases/29-fleet-metering/29-02-SUMMARY.md`
