---
depends_on: []
files_modified:
  - src/adapters/pi.ts
  - tests/adapters/pi-usage.test.ts
autonomous: true
must_haves:
  truths:
    - pi token usage is measured from ~/.pi/agent/sessions/<slug>/*.jsonl with
      the attempt-scoped per-record ISO timestamp cursor (A+B+C across an
      escalation, never 3A+2B+C)
    - The session-header cwd is verified against realpath(cwd) so slug-formula
      drift fails SAFE (unmetered), never mis-metered
    - usage.cost is never read; reasoning is never populated (usage.reasoning ⊂
      usage.output — would double-count in report total())
    - Any store anomaly ⇒ undefined (unmetered), never a throw, never {input:0}
---

```
<objective>
SPEND-10: implement `collectUsage(cwd, sinceMs)` on the pi adapter, reading assistant-message usage from pi's own cwd-slug-keyed session JSONL store.
</objective>
<done>Test file exists, references pi.collectUsage!, suite is RED (collectUsage not yet implemented).</done>
<done>pi-usage suite fully green (all drills incl. header-cwd fail-safe), full zero-token suite green (daemon.ts untouched), tsc clean.</done>
<context>
@.planning/phases/29-fleet-metering/29-RESEARCH.md
@src/adapters/claude-code.ts
@src/adapters/types.ts
@tests/adapters/claude-usage.test.ts
</context>
<task>
<task>
```
Create `.planning/phases/29-fleet-metering/29-03-SUMMARY.md`
