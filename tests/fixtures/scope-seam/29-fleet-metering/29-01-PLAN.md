---
depends_on: []
files_modified:
  - src/adapters/codex.ts
  - tests/adapters/codex-usage.test.ts
autonomous: true
must_haves:
  truths:
    - A 3-attempt codex escalation in one worktree meters per-attempt token
      deltas (A+B+C), never the cumulative total (3A+2B+C)
    - codex usage comes only from ~/.codex/sessions rollout JSONL — never a
      pane, never a trailer, never total_token_usage
    - Any store anomaly (missing dir, torn line, info:null, bad timestamp, no
      cwd match) yields undefined (unmetered), never a throw and never {input:0}
    - The returned TokenUsage never populates reasoning (reasoning_output_tokens
      ⊂ output_tokens — populating it double-counts in report total())
---

```
<objective>
SPEND-07: implement `collectUsage(cwd, sinceMs)` on the codex adapter, reading per-turn token DELTAS from codex's own on-disk session store (`~/.codex/sessions/YYYY/MM/DD/rollout-*.jsonl`), attempt-scoped by each record's own timestamp, tokens only.
</objective>
<done>Test file exists, references codex.collectUsage!, and the suite is RED (collectUsage not yet implemented). This is the red-capable oracle SPEND-07's falsification discipline requires.</done>
<done>codex-usage suite fully green (including all three drills), full zero-token suite green (tests/run/telemetry.test.ts daemon-fold byte pin untouched — daemon.ts NOT modified), tsc clean.</done>
<context>
@.planning/phases/29-fleet-metering/29-RESEARCH.md
@src/adapters/claude-code.ts
@src/adapters/types.ts
@tests/adapters/claude-usage.test.ts
</context>
<task>
<task>
```
Create `.planning/phases/29-fleet-metering/29-01-SUMMARY.md`
