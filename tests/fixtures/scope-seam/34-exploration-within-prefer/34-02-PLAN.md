---
depends_on:
  - 34-01
files_modified:
  - src/route/router.ts
  - tests/route/prefer-explore.test.ts
autonomous: true
must_haves:
  truths:
    - "An implement task under the default config with an under-observed codex
      cell routes to codex:gpt-5.6-terra with probe provenance and deviation {
      explore: true } — proven RED against the pre-fix router first (O-1)"
    - drover plan output names the within-prefer exploration pick (provenance
      'via exploration probe' + the ⇄ deviation line) before any real run (O-7
      preview-before-bite)
    - Cold/empty profile still routes prefer[0] byte-identical for every config
      — all of Plan 01's baselines stay green unmodified
    - "Probe budget self-extinguishes: at EXPLORE_CAP dispatches the bonus is 0
      and cold order resumes; routing is deterministic (repeated route()
      deep-equal, no RNG)"
    - A warm within-prefer score difference with zero bonuses produces a
      deviation WITHOUT explore and 'via learned score' provenance (marker
      precision)
---

```
<objective>
Fix PREFER-STARVATION (ROUTE-17): replace the profile-branch learned sort (router.ts:114) with the group-representative key vector from 34-RESEARCH.md §The Design, and rewrite the marker block (:116-126) as first-differing-key semantics so `explore: true` fires for within-prefer probes.
</objective>
<context>
@.planning/phases/34-exploration-within-prefer/34-RESEARCH.md
@.planning/phases/34-exploration-within-prefer/34-VALIDATION.md
@src/route/router.ts
@src/route/profile.ts
@src/cli/commands/plan.ts
@tests/route/prefer-explore.test.ts
</context>
<task>
<task>
<task>
```

