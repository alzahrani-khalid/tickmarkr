---
depends_on: []
files_modified:
  - tests/route/prefer-explore.test.ts
autonomous: true
must_haves:
  truths:
    - Cold/empty profile routes prefer[0] for every prefer-hinted shape,
      deep-equal to the 3-arg route() call, including the tier-asymmetric
      overlay prefer:['claude-code:fable','codex'] (ROUTE-07)
    - An off-prefer channel with maximal exploration bonus and positive learned
      score never beats an in-prefer channel (containment)
    - Task/map pins never consult the profile (reads === 0) and floors still
      filter before the learned sort (ROUTE-08)
    - Within one prefer entry (intra-group), marginal cost and tier still
      outrank bonus/score
    - A pinned warm non-prefer routing case reproduces today's exact Route object
---

```
<objective>
Pin the four invariants that the Phase 34 comparator change must NOT break, in a NEW oracle file, BEFORE the router is touched (wave 0 baselines per 34-VALIDATION.md).
</objective>
<context>
@.planning/phases/34-exploration-within-prefer/34-RESEARCH.md
@.planning/phases/34-exploration-within-prefer/34-VALIDATION.md
@src/route/router.ts
@src/route/profile.ts
@tests/route/explore.test.ts
@tests/route/parity.test.ts
</context>
<task>
<task>
```
Create `tests/route/prefer-explore.test.ts`
